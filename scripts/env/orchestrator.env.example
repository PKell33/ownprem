# Nodefoundry Orchestrator Environment Configuration
# Copy to /etc/nodefoundry/orchestrator.env and customize

# Server port
PORT=3001

# Database path
DATABASE_PATH=/var/lib/nodefoundry/db.sqlite

# Data directories
DATA_PATH=/var/lib/nodefoundry
APPS_PATH=/opt/nodefoundry/apps
APP_DEFINITIONS_PATH=/opt/nodefoundry/repo/app-definitions
LOGS_PATH=/var/log/nodefoundry

# REQUIRED: Encryption key for secrets (32+ characters)
# Generate with: openssl rand -base64 32
SECRETS_KEY=

# REQUIRED: JWT secret for authentication (32+ characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# CORS origin (set to your domain in production)
# Example: https://foundry.yourdomain.com
CORS_ORIGIN=

# Logging level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info
